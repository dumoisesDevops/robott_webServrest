name: Run Robot Framework Tests with Selenium and Chrome (Windows)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests:
    runs-on: windows-latest  # Alterado para Windows

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install robotframework
          pip install robotframework-seleniumlibrary
          pip install faker
          pip install robotframework-faker

      - name: Install Google Chrome
        run: |
          choco install googlechrome  # Usando o Chocolatey para instalar o Chrome

      - name: Install ChromeDriver
        run: |
          choco install chromedriver  # Usando o Chocolatey para instalar o ChromeDriver

      - name: Check Google Chrome version
        run: |
          google-chrome --version

      - name: Check ChromeDriver version
        run: |
          chromedriver --version

      - name: List files for debugging
        run: |
          dir /s  # Verifica a estrutura do repositório para garantir que 'tests/features' existe

      - name: Run Robot Framework Tests
        run: |
          robot --variable BROWSER:Chrome --variable HEADLESS_MODE:"--headless --no-sandbox --disable-dev-shm-usage" -d results projeto-automacao/tests/features/

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: robot-results
          path: results
