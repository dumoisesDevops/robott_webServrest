name: Run Robot Framework Tests with Selenium and Chrome (Windows)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-tests:
    runs-on: windows-latest  # Usando Windows como sistema operacional

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install robotframework
          pip install robotframework-seleniumlibrary
          pip install faker
          pip install robotframework-faker

      - name: Verify Selenium Installation
        run: |
          pip show selenium  # Verifica se o Selenium está instalado

      - name: Install Google Chrome
        run: |
          choco install googlechrome -y  # Instalando o Google Chrome com o Chocolatey

      - name: Install ChromeDriver
        run: |
          choco install chromedriver -y  # Instalando o ChromeDriver com o Chocolatey

      - name: Check Google Chrome version
        run: |
          "C:\Program Files\Google\Chrome\Application\chrome.exe" --version  # Usando o caminho completo para o Chrome

      - name: Check ChromeDriver version
        run: |
          chromedriver --version

      - name: List files for debugging
        run: |
          dir /s  # Verifica a estrutura do repositório para garantir que 'tests/features' existe

      - name: Run Robot Framework Tests
        run: |
          robot --variable BROWSER:Chrome --variable HEADLESS_MODE:"--headless --no-sandbox --disable-dev-shm-usage" -d results projeto-automacao/tests/features/

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: robot-results
          path: results
